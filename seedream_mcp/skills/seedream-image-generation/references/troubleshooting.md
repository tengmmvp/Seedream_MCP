# 故障排查

本文件是 `seedream-image-generation` skill 的参考文档，覆盖错误码对策、常见失败模式与配额约束。

## 错误码速查

工具返回的错误消息形如 `标题: 原因 [错误码: X] 处置建议`，按下表处置：

| 状态码 | 含义         | 下一步动作                                       |
| ------ | ------------ | ------------------------------------------------ |
| 400    | 请求参数错误 | 核对参数取值：size 格式、参考图数量、prompt 长度 |
| 401    | 认证失败     | API 密钥无效或过期，请用户检查 `ARK_API_KEY`     |
| 402    | 余额不足     | 请用户检查账户余额与配额                         |
| 403    | 权限不足     | API Key 未开通目标模型权限，换模型或开通授权     |
| 413    | 请求体过大   | 减小参考图尺寸，或改用 URL 而非 Base64 传入      |
| 422    | 参数语义错误 | 按错误消息逐项核对字段含义                       |
| 429    | 频率超限     | 稍等后重试；错误消息带建议等待秒数时遵照执行     |
| 5xx    | 服务端错误   | 稍后重试，连续失败请用户查看日志                 |

## 常见失败模式与对策

- **引用的图片 URL 已失效**：API 返回的 URL 仅保留 24 小时。对策：改用本地保存路径（`browse_images` 定位），或重新生成
- **尺寸档位被拒**：不同模型支持的档位与像素区间不同（如 5.0 Pro 档位更少且有 16 像素倍数约束）。对策：读 `seedream://models/info` 拿当前模型的 `allowed_presets`、`min/max_size_pixels` 后重新取值
- **能力不支持**：
  - 5.0 Pro 调组图/联网搜索/流式 → 换 5.0 系列
  - 4.5/4.0 传 `output_format` 或 `optimize_prompt_options` → 去掉该参数
  - 非 5.0 Pro 调 `layer_decomposition`/`background` → 换 5.0 Pro
- **413 请求体过大**：Base64 传图体积膨胀明显。对策：压缩或缩小图片后重传；有公网可达地址时改用 URL
- **5.0-lite 组图张数报错**：约束是"参考图数量 + 生成数量 ≤ 15"，调低 `max_images` 或减少参考图
- **生成成功但未找到保存文件**：确认 `auto_save` 未被显式关闭；按保存路径规则查找（见 SKILL.md"图片的保存与复用"）

## 输入与配额约束

- 输入图单张不超过 30 MB；支持 jpeg/png/webp/bmp/tiff/gif/heic/heif（图层拆分场景仅 png/jpeg）
- 生成场景输入图总像素 196 ~ 3600 万，宽高比 1/16 ~ 16；宽高须大于 14 px
- 参考图上限：5.0 Pro 最多 10 张，其余模型最多 14 张
- 结果预览：自动保存成功时附带缩略图（长边 ≤768px，最多 10 张），纯预览用途，引用请用保存路径
- 保存目录 30 天自动清理、总量 10GB 上限（超限按最旧驱逐），重要图片提醒用户归档
- 限流按"账号 + 模型"每分钟图片数计；图层拆分预扣 17 IPM

## 诊断信息

- `seedream://server/info`：服务器版本、当前模型、默认尺寸、自动保存开关
- `seedream://models/info`：全部模型别名与能力声明（档位、像素区间、参考图上限、能力开关）
- 日志文件：`<进程工作目录>/.seedream/logs/seedream_mcp.log`，保留 30 天，错误日志含完整堆栈
